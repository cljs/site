(ns util.highlight)

;; replace with rewrite-clj
;; then highlight core symbols and special forms
;; then highlight the rest based on reader types (e.g. keywords, strings)

(defn highlight-code [code lang]
  code)
